import { createFileRoute } from "@tanstack/react-router";
import { Check, Copy, ExternalLink, ShieldAlert } from "lucide-react";
import { Button } from "@/components/ui/button";
import { pageMeta } from "@/lib/colony-data";
export const Route = createFileRoute("/buy")({ head: () => pageMeta("Buy $GrokX", "Placeholder guide to acquiring $GrokX on Robinhood Chain and pairing with $SPCX."), component: BuyPage });
function BuyPage() { const copy = () => navigator.clipboard?.writeText("CONTRACT_ADDRESS_PENDING"); return <main className="mx-auto max-w-[1100px] px-5 py-12"><p className="font-mono text-xs text-primary">OUTPOST MARKET · EXPERIMENTAL</p><h1 className="mt-2 text-3xl font-black md:text-5xl">Buy $GrokX</h1><p className="mt-4 max-w-2xl text-lg text-secondary-foreground">The colony token on Robinhood Chain, paired with $SPCX. Secondary to life on Mars. Still useful for the market.</p><div className="mt-10 grid gap-10 lg:grid-cols-[1fr_340px]"><section><div className="divide-y divide-border border-y border-border">{[["01","Prepare a compatible wallet","Confirm Robinhood Chain support before moving anything valuable."],["02","Fund with $SPCX","Acquire the pair asset and leave room for network fees."],["03","Open the verified market","Use the official market link once the contract is published."],["04","Swap for $GrokX","Review the pair, amount, and slippage. Then review them again."]].map(([n,t,c]) => <article key={n} className="grid gap-4 py-7 sm:grid-cols-[48px_1fr]"><span className="font-mono text-accent">{n}</span><div><h2 className="font-bold">{t}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{c}</p></div></article>)}</div></section><aside className="h-fit border border-border bg-surface p-5"><p className="font-mono text-[10px] text-muted-foreground">PAIR</p><p className="mt-2 text-2xl font-bold">$GrokX / $SPCX</p><p className="mt-6 font-mono text-[10px] text-muted-foreground">CONTRACT ADDRESS</p><div className="mt-2 flex items-center border border-border bg-background p-2"><code className="min-w-0 flex-1 truncate text-xs text-secondary-foreground">PENDING_VERIFICATION</code><Button variant="ghost" size="icon" onClick={copy} aria-label="Copy contract address"><Copy /></Button></div><Button disabled className="mt-5 w-full">Market link pending <ExternalLink /></Button><div className="mt-6 flex gap-3 border-t border-border pt-5"><ShieldAlert className="size-5 shrink-0 text-warning" /><p className="text-xs leading-5 text-muted-foreground">Experimental token. This page is a placeholder, not financial advice. Verify the final contract before interacting.</p></div><p className="mt-5 flex items-center gap-2 font-mono text-[10px] text-accent"><Check className="size-3" />ROBINHOOD CHAIN</p></aside></div></main>; }