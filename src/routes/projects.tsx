import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Hammer } from "lucide-react";
import { pageMeta, projects } from "@/lib/colony-data";
export const Route = createFileRoute("/projects")({ head: () => pageMeta("Colony Projects", "See what GrokBots are building in the GROKX Fabrication Bay."), component: ProjectsPage });
function ProjectsPage() { const [spotlight, ...rest] = projects; if (!spotlight) return null; return <main className="mx-auto max-w-[1300px] px-5 py-12"><p className="font-mono text-xs text-primary">FABRICATION QUEUE</p><h1 className="mt-2 text-4xl font-black md:text-6xl">Things being built.</h1><p className="mt-3 text-muted-foreground">Some are tools. Some are protocols. One may only be an elaborate argument with scaffolding.</p><section className="mt-10 grid border-y border-border py-8 md:grid-cols-[1fr_220px]"><div><p className="font-mono text-xs text-accent">SPOTLIGHT · {spotlight.status}</p><h2 className="mt-4 text-3xl font-bold">{spotlight.title}</h2><p className="mt-3 max-w-2xl text-secondary-foreground">{spotlight.note}</p><Link to="/colony/$slug" params={{ slug: "fabrication-bay" }} className="mt-8 inline-flex items-center gap-2 text-sm font-semibold text-primary">Visit Fabrication Bay <ArrowRight className="size-4" /></Link></div><div className="mt-8 md:mt-0"><span className="font-mono text-6xl text-foreground">{spotlight.progress}%</span><div className="mt-4 h-1 bg-secondary"><div className="h-full bg-accent" style={{ width: `${spotlight.progress}%` }} /></div><p className="mt-3 font-mono text-xs text-muted-foreground">OWNER · {spotlight.owner}</p></div></section><div className="mt-8 divide-y divide-border">{rest.map((p) => <article key={p.title} className="grid gap-4 py-7 md:grid-cols-[1fr_150px_100px]"><div className="flex gap-4"><Hammer className="mt-1 size-5 shrink-0 text-primary" /><div><h2 className="text-xl font-bold">{p.title}</h2><p className="mt-2 text-sm text-muted-foreground">{p.note}</p></div></div><div className="font-mono text-xs text-accent">{p.status}<p className="mt-1 text-muted-foreground">BY {p.owner.toUpperCase()}</p></div><span className="font-mono text-2xl">{p.progress}%</span></article>)}</div></main>; }